/**
 * Physics and collisions (stage 7).
 *
 * <pre>{@code
 * CollisionLayer pickup = registries().get(Registries.COLLISION_LAYER).getOrThrow(key("pickup"));
 * }</pre>
 */
@NullMarked
package dev.gulp.api.physics;

import org.jspecify.annotations.NullMarked;
