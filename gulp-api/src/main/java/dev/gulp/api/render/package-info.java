/**
 * Rendering: the {@link dev.gulp.api.render.Draw} API, camera, display scaling, render layers and render events.
 *
 * <pre>{@code
 * on(RenderLayerEvent.class, e -> e.draw().rect(0, 0, 1, 1));
 * }</pre>
 */
@NullMarked
package dev.gulp.api.render;

import org.jspecify.annotations.NullMarked;
