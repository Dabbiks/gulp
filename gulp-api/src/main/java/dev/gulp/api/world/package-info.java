/**
 * Worlds, tile maps and chunks (stage 6).
 *
 * <pre>{@code
 * TileType grass = registries().get(Registries.TILE_TYPE).getOrThrow(key("grass"));
 * }</pre>
 */
@NullMarked
package dev.gulp.api.world;

import org.jspecify.annotations.NullMarked;
