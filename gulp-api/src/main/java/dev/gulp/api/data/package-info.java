/**
 * Data: JSON values, codecs, typed data containers and YAML configuration.
 *
 * <pre>{@code
 * int lives = config().getInt("player.lives", 3);
 * }</pre>
 */
@NullMarked
package dev.gulp.api.data;

import org.jspecify.annotations.NullMarked;
