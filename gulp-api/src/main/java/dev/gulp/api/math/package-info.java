/**
 * 2D math: vectors, shapes, transforms, easing, curves, intersections, geometry, random numbers and noise. The Y axis
 * points down and angles are in degrees.
 *
 * <pre>{@code
 * Vec2 toTarget = target.sub(position).clampLength(maxStep);
 * }</pre>
 */
@NullMarked
package dev.gulp.api.math;

import org.jspecify.annotations.NullMarked;
