/**
 * User interface built in code (stage 9).
 *
 * <pre>{@code
 * Theme dark = registries().get(Registries.THEME).getOrThrow(Key.parse("gulp:dark"));
 * }</pre>
 */
@NullMarked
package dev.gulp.api.ui;

import org.jspecify.annotations.NullMarked;
