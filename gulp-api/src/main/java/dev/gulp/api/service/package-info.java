/**
 * Services shared between modules through interfaces, with priorities and ownership.
 *
 * <pre>{@code
 * Economy economy = services().getOrThrow(Economy.class);
 * }</pre>
 */
@NullMarked
package dev.gulp.api.service;

import org.jspecify.annotations.NullMarked;
