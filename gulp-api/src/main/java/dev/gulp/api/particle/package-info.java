/**
 * Particle effects: {@link dev.gulp.api.particle.ParticleEffect} made of
 * {@link dev.gulp.api.particle.EmitterConfig emitters}, spawned with {@code world.spawnParticles}.
 *
 * <pre>{@code
 * ParticleInstance sparks = world.spawnParticles(assets().get(GameAssets.Particles.SPARKS), hit.position());
 * }</pre>
 */
@NullMarked
package dev.gulp.api.particle;

import org.jspecify.annotations.NullMarked;
