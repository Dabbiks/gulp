/**
 * Particle effects (stage 8).
 *
 * <pre>{@code
 * ParticleEffect sparks = registries().get(Registries.PARTICLE_EFFECT).getOrThrow(key("sparks"));
 * }</pre>
 */
@NullMarked
package dev.gulp.api.particle;

import org.jspecify.annotations.NullMarked;
