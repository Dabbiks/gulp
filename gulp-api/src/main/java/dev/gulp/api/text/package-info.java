/**
 * Text: fonts (MSDF, bitmap, dynamic), styles, rich text with markup and animated effects, and layout with wrapping.
 *
 * <pre>{@code
 * draw.text(Text.markup("[b]Wynik:[/b] [color=gold]120[/color]"), 8, 8, TextStyle.of(20));
 * }</pre>
 */
@NullMarked
package dev.gulp.api.text;

import org.jspecify.annotations.NullMarked;
