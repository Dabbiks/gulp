/**
 * Entities, entity types and components (stage 6).
 *
 * <pre>{@code
 * EntityType player = registries().get(Registries.ENTITY_TYPE).getOrThrow(key("player"));
 * }</pre>
 */
@NullMarked
package dev.gulp.api.entity;

import org.jspecify.annotations.NullMarked;
