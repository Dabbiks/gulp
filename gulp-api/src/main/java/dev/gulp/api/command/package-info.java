/**
 * Commands with typed arguments and the developer console.
 *
 * <pre>{@code
 * command("hello", ctx -> ctx.reply("Hello!"));
 * }</pre>
 */
@NullMarked
package dev.gulp.api.command;

import org.jspecify.annotations.NullMarked;
