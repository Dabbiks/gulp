/**
 * Lifecycle events fired by the engine: game start and stop, modules, ticks, pause, focus and window size.
 *
 * <pre>{@code
 * on(GameStartEvent.class, e -> logger().info("Ready"));
 * }</pre>
 */
@NullMarked
package dev.gulp.api.event.lifecycle;

import org.jspecify.annotations.NullMarked;
