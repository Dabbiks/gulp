/**
 * Web backend on TeaVM (Wasm GC with JS fallback): WebGL2, WebAudio, DOM input, IndexedDB. Implemented in roadmap
 * stage 3; the module exists from stage 0 so the build layout is final.
 */
@NullMarked
package dev.gulp.backend.web;

import org.jspecify.annotations.NullMarked;
