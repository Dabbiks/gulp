/**
 * Annotation processor generating event dispatchers ({@code <Listener>$Handlers}), component codecs for
 * {@code @Serializable} records and compile-time module validation. Implemented in roadmap stage 1.
 */
@NullMarked
package dev.gulp.processor;

import org.jspecify.annotations.NullMarked;
